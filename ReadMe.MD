Python project:
Desktop applicaiton for books invenotry with Tkinter

database:  sqlite

# To convert the code into executable application(.exe)
pip install pyinstaller

pyinstaller --onefile --windowed BookRecordPro.py